import PlaceholderScreen from '@/components/PlaceholderScreen';

export default function AddProductScreen() {
  return (
    <PlaceholderScreen
      title="Add Product"
      description="Name, category, stock, price, GST rate, HSN code and optional brand."
      buildsIn="Phase 2 (T2.3)"
    />
  );
}
